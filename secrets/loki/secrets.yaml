# secrets/loki/secrets.yaml — Loki container secrets template
#
# ⚠️  THIS FILE IS A TEMPLATE — NOT YET ENCRYPTED ⚠️
#
# Before committing real values:
# 1. Configure .sops.yaml with Loki's age public key
# 2. Replace PLACEHOLDER values with real secrets
# 3. Encrypt: sops --encrypt --in-place secrets/loki/secrets.yaml
# 4. Verify: sops --decrypt secrets/loki/secrets.yaml
#
# Usage in NixOS modules (after sops-nix is wired):
#   sops.secrets."openclaw/anthropic_api_key" = {};
#   services.openclaw.secretsFile = config.sops.secrets."openclaw-env".path;

openclaw:
    anthropic_api_key: PLACEHOLDER_ENCRYPT_ME
    # discord_token: PLACEHOLDER
    # openai_api_key: PLACEHOLDER
    # brave_api_key: PLACEHOLDER
