# sops-encrypted secrets template
# Encrypt with: sops --encrypt --in-place secrets.yaml
# Decrypt with: sops --decrypt secrets.yaml
#
# Before using: configure .sops.yaml with your age public key
# Generate age key: age-keygen -o /root/.config/sops/age/keys.txt

openclaw:
    anthropic_api_key: PLACEHOLDER_ENCRYPT_ME
    # Add other API keys below
    # openai_api_key: PLACEHOLDER
    # discord_token: PLACEHOLDER
